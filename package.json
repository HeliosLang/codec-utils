{
    "name": "@helios-lang/codec-utils",
    "version": "0.1.6",
    "description": "Primitive manipulation functions commonly used in encoding and decoding algorithms",
    "main": "src/index.js",
    "type": "module",
    "scripts": {
        "prebuild": "npm run clean && npx tsc -p jsconfig.json --noEmit false --emitDeclarationOnly",
        "build": "npm test",
        "clean": "find src/ -name *.d.ts* | xargs rm -f",
        "prettify": "npx prettier . --write",
        "prepare": "npm run build",
        "postpublish": "npm run clean",
        "test": "npx prettier . --check && node --test  --experimental-test-coverage"
    },
    "author": "Christian Schmitz",
    "license": "BSD-3-Clause",
    "prettier": {
        "trailingComma": "none",
        "tabWidth": 4,
        "semi": false,
        "singleQuote": false
    },
    "devDependencies": {
        "@types/node": "^20.10.0",
        "prettier": "^3.1.0",
        "typescript": "^5.3.2"
    }
}
